{
  "name": "qm-idlogin-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal OIDC authorization server: a single id input signs the user straight in. Speaks the authorization-code + PKCE subset the portal consumes.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "start": "node src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "jose": "^6.2.3",
    "pg": "^8.13.1"
  }
}
